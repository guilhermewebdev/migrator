#!/bin/bash

ROOT_DIR=$(realpath "$(dirname $0)/..")
source $ROOT_DIR/bin/.env
ROOT_DIR=$ROOT_DIR CGO_ENABLED=0 go build -v -ldflags="-s -w" -o $ROOT_DIR/bin/migrate $@ $SRC_DIR
du -h $ROOT_DIR/bin/migrate
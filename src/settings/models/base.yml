# Migrator settings
# Generated by version Migrator v0.2
# Change the information below accordingly with your necessity

# Directory where migration files are stored
migrations_dir: ./migrations

# Name of the table to track migrations in the database
migrations_table_name: migrations

# Database connection string (DSN)
# Uncomment a specific DSN that you need

# Postgres DSN
db_dsn: postgres://$DB_USER:$DB_PASS@$DB_HOST:$DB_PORT/$DB_NAME

# MySQL DSN
# db_dsn: $DB_USER:$DB_PASS@tcp($DB_HOST:$DB_PORT)/$DB_NAME

# MS SQL Server DSN
# db_dsn: sqlserver://$DB_USER:$DB_PASS@$DB_HOST:$DB_PORT?database=$DB_NAME

# SQLite DSN 
# db_dsn: $CWD/db.sqlite3

# Database driver (Supported drivers: mysql, postgres, sqlserver, sqlite, sqlite3, oracle)
db_driver: postgres